## 请帮我设计一个私人的图床应用HMiMG

### 技术栈

Nodejs+TypeScript+React+Next.js+Koa+SQL，包管理工具使用pnpm

### 功能简介：

I. 用户

用户的数量可设置（此功能请做进前端后台和后端，而不是数据库逻辑），其中一个是管理员，默认只生成管理员账号，可更改管理员账号、密码。默认管理员账号、密码、邮箱、手机号分别为admin,admin,admin@yourdomaname.com,+8613200000000。管理员有开启注册权限，但是如果用户数已经超出规定值以后，不得开放注册。

II. 存储逻辑

三个单位：图片、相册、合集
图片必须包含在相册中，图片与相册的对应关系是多对一。
合集与相册的对应关系是多对多
合集只可以包含合集或者相册的一种。不可合集内包含合集和相册。
合集最多嵌套三层，举例最外面一层为合集、下一层是合集、再下一层只能是相册。

III. 前端逻辑

首页在用户登录状态显示网站Logo，查看相册、查看合集、进入后台以及上传功能，未登录自动跳转登录界面。
后台可管理相片、相册和合集。首次运行检查数据库链接。关于数据库请提前生成一个config文件。
前端可调节明暗模式，请尽可能设计的比较简约。

IV. 数据库逻辑

图片只存储在后端硬盘，数据库存储用户、图片、相册和合集的对应关系以及其他信息。本地开发环境只提供MySQL(8.4.8 LTS)数据库，请在生成MySQL逻辑后再写一份PostgreSQL的代码。数据表前缀为“HMIMG_”
 
请先帮我生成相应代码，等代码生成完毕、提供config文件后，我填入数据库信息后再进行测试。